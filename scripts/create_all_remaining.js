/**
 * Code Debugger - Build Remaining Mode Files (11 files, 330 questions)
 */

const fs = require('fs');
const path = require('path');
const MODES_DIR = path.join(__dirname, '..', 'game', 'js', 'modes');

console.log('Writing remaining mode banks to', MODES_DIR);
